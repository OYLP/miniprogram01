.tabs .tabs_title {
  display: flex;
  padding: 15rpx 0;
}
.tabs .title_item {
  display: flex;
  flex: 1;
  justify-content: center;
  align-items: center;
}
.tabs .active {
  color: var(--themeColor);
  border-bottom: 5rpx solid currentColor;
}
